runtime: nodejs16

instance_class: F2

env_variables:
  BUCKET_NAME: "putaringonit-angular-bucket"

handlers:
  - url: /
    static_files: dist/index.html
    upload: dist/index.html

  - url: /
    static_dir: dist
